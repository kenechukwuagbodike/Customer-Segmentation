# Python
.venv/
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/

# Environment & secrets
.env
*.env

# Raw + intermediate data: large, regenerate locally via the pipeline scripts.
# Small derived files (segments.csv, segment_profiles.csv, elbow_data.csv,
# segment_products.csv) are NOT excluded. The deployed Streamlit app reads
# them directly and Streamlit Cloud never runs the pipeline itself.
data/online_retail_II.xlsx
data/clean.csv
data/rfm.csv
data/*.parquet
data/*.db
*.db

# models/*.pkl and models/*.joblib are committed on purpose, same reason
# as above, the dashboard needs them and there's no build step to regenerate them

# Logs
*.log
run_log.txt

# VS Code
.vscode/

# macOS
.DS_Store

# Windows
Thumbs.db
desktop.ini

# Jupyter
.ipynb_checkpoints/
